pkgname=minigrep-bin
pkgver=1.0.0
pkgrel=1
pkgdesc="Lightweight version of grep (prebuilt binary)"
arch=('x86_64')
url=""
license=('MIT')
depends=()

package() {
  install -Dm755 minigrep "$pkgdir/usr/bin/minigrep"
}
